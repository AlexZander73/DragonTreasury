# Texture Overlays

Procedural texture overlays used for material richness:

- `metal-grain.svg`
- `gem-grain.svg`
- `dragon-hide.svg`

These are blended at runtime in Pixi layers for subtle non-flat surfaces.
